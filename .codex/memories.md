# Memories

2026-06-18: `ribs` workspace at `/Users/aklo/projects/ribs` is the Roots in Blue Stone (RIBS) band site — a client web project, redesign of rootsinbluestone.com (Wix), Spotify/dusk.fm app feel. Stack: Next.js 16 + React 19 + Tailwind v4 + shadcn/ui (Base UI, `base-nova`) — first AKLO project using shadcn. Design system is **Bluestone Dark** (deep slate-indigo `#0b0f1a`, frosted glass `.glass`/`.glass-raised`, warm amber `--primary` `#e0a84f`), defined in `app/globals.css`, ported from the `elegant` house style. Band = PA/Poconos duo Walter Lee + Ian Kirk (reggae/rock/blues/soul), scales duo→7-piece. Content is data-driven in `lib/content.ts` and `lib/tour.ts`; merch intentionally excluded.

Aesthetic (2026-06-18 redesign): **monochrome chrome** — black `#060606` base, near-white `#f5f5f6` primary, grayscale + metallic-chrome highlights, no blue/amber. Fonts: Syne display + Geist body + mono labels. Real assets scraped from Wix into `public/` (logo `img/logo.png` used as the wordmark, `img/hero-banner.jpg` grayscale hero, `img/one-last-breath.png` cover floats in hero right column, `gallery/g*.jpg`). Photography is grayscale via `.mono`; album cover left in original color as the single focal accent. Nav has no social icons (Tour Dates + Book Now buttons). Copy is kept to the band's real bio/site wording — avoid inventing marketing copy.

Priorities surfaced top-of-page: tour dates, latest release ("One Last Breath"), booking form. Booking = 4-step rhf+zod form (`components/booking/booking-form.tsx`, schema `lib/booking-schema.ts`) -> `POST /api/book` (edge) emails `rootsinbluestone@gmail.com` via Mailchimp Transactional when `MAILCHIMP_TRANSACTIONAL_API_KEY` or `MANDRILL_API_KEY` plus verified `BOOKING_FROM_EMAIL` are configured, with Resend fallback support. Name, email, city, and state/region are required; unconfigured booking email returns HTTP 503 with the direct email fallback instead of fake success. Music section uses a live Spotify artist embed.

Verified 2026-06-18: build + lint clean; browser walkthrough of all sections; full booking submit -> success state. GitHub: private repo `git@github.com:aklo360/ribs.git` (https://github.com/aklo360/ribs), `main` pushed. Deploy: Cloudflare Pages project `roots-in-blue-stone`, built with `@cloudflare/next-on-pages` (works on Next 16); staging LIVE at https://staging.roots-in-blue-stone.pages.dev (edge `/api/book` verified 200/422 on CF). Responsive verified live on iPhone/iPad/360px Android/desktop, no overflow. Pending (all approval-gated): real transactional booking-email sender secrets, production deploy to `main`/`roots-in-blue-stone.pages.dev`, DNS cutover from Wix. Do not run production deploy or contact externally without explicit approval.

2026-06-24: Walter Drive photo integration + live booking quote system implemented. Gallery now includes 19 existing photos, 13 optimized Walter Drive photos in `public/gallery/walter/`, and a curated 100-image optimized selection from the downloaded `Photos-20260628T054608Z-3-00x.zip` RIBS archives in `public/gallery/zips/`; raw local Drive staging is ignored at `refs/walter-drive/`. Booking form/schema now follows Walter's notes: Duo / 4-Piece / 5-Piece / 7-Piece, 2 hours / 3 hours / 3+ custom, outdoor shade/power prompts, no client-facing sound-engineer question, Original Music capped to 2 hours, and live narrow estimate from `lib/booking-quote.ts`; `/api/book` includes the same estimate in the email summary. Verified with lint/build and local browser checks.

2026-06-28: Latest release corrected to `Break Down` after verifying public Apple Music/iTunes metadata and Walter's Drive `RIBS SINGLE ART` folder via Drive-only `aklonyc@gmail.com` auth. Hero latest-release card now uses local 1200px Break Down artwork and is a 2.5D flip card: front cover art, back preview player. `components/site/player-provider.tsx` owns shared latest-release audio state so the hero player and sticky footer player control the same audio/progress/dismiss state. Music section is now a compact single-column 9-release carousel with local 1200px cover art in `public/img/releases/`, per-release preview player, icon-only platform buttons, outside chevrons, centered thumbnails, and no `One Day (Radio Edit)`. Sticky player hides/pauses while Music is visible.

2026-07-02: Gallery deduped for launch. The rendered `GALLERY` in `lib/content.ts` now shows 102 unique entries: Walter Drive versions replace lower-quality duplicate site photos, six unique `public/gallery/g*.jpg` site photos remain, the zip batch renders `ribs-zip-001` through `ribs-zip-084` except `ribs-zip-060`, and source image files were preserved. The gallery carousel uses the deduped reverse-chronological order without randomizing on refresh. The About section uses explicit `ABOUT_IMAGE = "/gallery/g09.jpg"` so the duo portrait is stable regardless of gallery ordering.

2026-07-03: Mobile hamburger menu logo distortion fixed by preventing the sheet flex column from stretching the logo image. `npm install` repaired `package-lock.json` after `npm ci` failed on missing `@emnapi` entries, and lint/build/Cloudflare Next-on-Pages build passed. Current worktree was deployed directly to Cloudflare Pages with `--branch staging`; live alias verified at https://staging.roots-in-blue-stone.pages.dev, including a mobile menu Playwright check showing rendered logo ratio matches natural ratio.

2026-07-03 follow-up: User rejected random gallery order. `components/site/gallery.tsx` now renders the ordered `GALLERY` list directly again, with no shuffle on mount/refresh. Lint/build and a local `next start` Playwright check confirmed the first gallery image stays `/gallery/walter/w05.jpg` across reloads; staging branch alias was redeployed afterward.

2026-07-08: Walter reported stale Bandsintown dates. The site was not live-pulling Bandsintown; `lib/tour.ts` held a static copied list. Direct unauthenticated Bandsintown API requests returned authorization errors and the public artist page was Cloudflare-blocked from the shell, so the launch list was manually synced from accessible Shazam/Bandsintown-derived event listings: 14 upcoming shows from 2026-07-10 through 2026-12-19. About "Available as" now reads Duo / 4 Piece / 5 Piece / 7 Piece with no Trio. Public-show quote minimums in `lib/booking-quote.ts` were lowered per Walter's note: Duo $500, 4-Piece $800, 5-Piece $1,000, 7-Piece $1,400. `plans/production-launch.md` captures the final production deploy checklist. `npm run lint` and `npm run build` passed. Weddings and Printful remain discussion/decision items before production launch.

2026-07-10: Custom 4+ hour booking estimates now increase both min and max as entered hours increase. Sound/PA selections are informational only and do not change quote ranges. The custom-hours and band-provided-sound explanatory quote pills are intentionally not emitted by `lib/booking-quote.ts`.

2026-07-10: Gallery curation pass removed the requested visible carousel positions from the rendered gallery without deleting source files. `lib/content.ts` now hides those mapped zip images through `ZIP_GALLERY_EXCLUSIONS`, leaving 66 visible gallery images.

2026-07-10: Break Down cover art now drives `app/favicon.ico`; generated `app/opengraph-image.tsx`/`app/twitter-image.tsx` social cards use a simplified black hero-style card with the band logo and only the visible text `Groove, Grit & Good Vibes`. Music carousel playback now uses the shared `PlayerProvider`, so Music, hero, and sticky footer controls stay synchronized. `Santa Claus Is Coming To Town` is intentionally removed from the Music carousel. This launch batch was deployed to the Cloudflare Pages `staging` alias on 2026-07-10 and verified with HTTP 200.

2026-07-10: Share metadata now resolves canonical/OG/Twitter hosts from build-time `NEXT_PUBLIC_SITE_URL` or `CF_PAGES_URL`; direct staging uploads must be built with `NEXT_PUBLIC_SITE_URL=https://staging.roots-in-blue-stone.pages.dev`. Exported metadata uses hyphens/prose instead of em dashes. Latest staging deploy verified live metadata points OG/Twitter images at the staging host.

2026-07-10: Share-card correction after client review: root metadata title is `Roots in Blue Stone · Originals & Covers · Groove, Grit & Good Vibes`; app title templates use middle-dot separators. `public/img/social/og-card.png` and `public/img/social/twitter-card.png` are static 1200x630 PNGs, regenerated by `scripts/generate-og-card.mjs` with a bounded headless Chrome screenshot so the tagline uses the same Syne font as the website. The card restores the Break Down cover art on the right, keeps the logo on the left, and avoids the plain logo/text-only layout.

2026-07-10: Future RIBS automation request captured: build a Bandsintown sync/listener for Roots in Blue Stone artist ID `15511983` so new shows update the website automatically instead of relying on manual `lib/tour.ts` edits. Current site remains static until this integration is designed and approved.

2026-07-10: Bandsintown sync/listener implementation plan added at `plans/bandsintown-sync-listener.md`. The selected path is a review-safe Node/GitHub Actions sync that keeps `lib/tour.ts` as the site source of truth, validates normalized events, and opens a PR when Bandsintown event data changes. The later final scrape found a working all-events widget endpoint, so true unattended pulls no longer depend on a custom source secret.

2026-07-10: Bandsintown reviewed sync implemented locally. New scripts: `scripts/tour-lib.mjs`, `scripts/tour-sync.mjs`, `scripts/notify-llphant-tg.mjs`, and fixtures under `scripts/fixtures/`. New workflow: `.github/workflows/bandsintown-sync.yml`, scheduled daily and manually runnable, opens/updates an automated PR when the default all-events feed or `BANDSINTOWN_EVENTS_URL` override yields validated changes and sends an LLPhant Telegram alert if bot/chat secrets are configured. Default sync mode merge-preserves existing shows; use workflow input/CLI `--replace` only for deliberate full-source replacement.

2026-07-10: RIBS GitHub Secrets now include LLPhant Telegram alert wiring by name: `TELEGRAM_BOT_TOKEN_LLPHANT`, `LLPHANT_TG_CHAT_ID`, and `LLPHANT_TG_THREAD_ID`. No secret values were printed and no live Telegram alert was sent during setup. Later the working all-events Bandsintown widget endpoint was found, so `BANDSINTOWN_EVENTS_URL` is now only an optional override.

2026-07-10: Final Bandsintown all-events scrape found the working widget endpoint `https://rest.bandsintown.com/V3.1/artists/Roots%20in%20Blue%20Stone/events/?app_id=js_roots-in-blue-stone.pages.dev`, which returns all 15 upcoming events. `lib/tour.ts` now includes the missing 2026-07-25 White Haven, PA `RIBS @ Jam Below The Dam` event ID `107919906`. The workflow/npm scripts use that all-events feed by default; `BANDSINTOWN_EVENTS_URL` is optional override only. Full-feed sync reports 15 current / 15 incoming / 0 changes after the correction.

2026-07-10: Tour cards now render a small ticket price chip beside each RSVP button. Current launch labels: all shows are `Free` except 2026-07-26 Mountain View Vineyard, Winery & Brewery, which is `$20`. `lib/tour.ts` owns these labels through `priceLabel`; the Bandsintown sync preserves manual paid labels and imports free markers when the feed reports them. RIBS also now supports the official Bandsintown for Artists API through the GitHub Secret `BANDSINTOWN_APP_ID` for artist ID `15511983`, falling back to the verified widget feed when that secret is absent. Do not commit the API key value.

2026-07-12: Virtual tip jar added to the RIBS site. The public payment destination is centralized at `SITE.tipJar` in `lib/content.ts` and currently points to the Venmo profile `https://www.venmo.com/u/waltanamo`, which was visible from the band's public Linktree. The homepage renders a dedicated tip-jar band after tour dates, and compact tip CTAs appear in the hero, booking contact chips, footer, and nav.

2026-07-13 follow-up: The virtual tip jar is paused for launch. The visible tip-jar section, nav link, hero CTA, booking chip, footer CTA, `SITE.tipJar`, and tip-jar components were removed from the app. Do not re-add the Venmo tip jar unless AKLO explicitly asks.

2026-07-13: Newsletter signup is now wired through a server-side Mailchimp Edge route at `app/api/newsletter/route.ts` instead of the old public embedded-form URL pattern. Cloudflare Pages needs `MAILCHIMP_API_KEY` and `MAILCHIMP_AUDIENCE_ID` secrets for live delivery; optional vars are `MAILCHIMP_SERVER_PREFIX`, `MAILCHIMP_SUBSCRIBE_STATUS` (default `pending` double opt-in), and `MAILCHIMP_TAGS` (default `Website Signup`). Do not expose Mailchimp credentials through `NEXT_PUBLIC_*`.

2026-07-13 follow-up: The previous Wix site's public Mailchimp connected-site script now lives at `SITE.mailchimpConnectedSiteScript` and is loaded once from `app/layout.tsx` with `next/script` / `afterInteractive`. This is only for Mailchimp site recognition/tracking; the visible newsletter form remains the custom RIBS UI and still submits through `/api/newsletter`.

2026-07-13 Mailchimp config: Cloudflare Pages production secrets for project `roots-in-blue-stone` now include `MAILCHIMP_API_KEY` and `MAILCHIMP_AUDIENCE_ID`, pointing at the Roots In Blue Stone Mailchimp audience. Secret values were not printed or committed. If staging/preview requires newsletter testing before production, verify whether Cloudflare Pages preview has access to the same secret set or configure it through the dashboard.

2026-07-13 production/staging split: Cloudflare Pages project `roots-in-blue-stone` now has a current production `main` deployment at `https://roots-in-blue-stone.pages.dev` and a separate development preview at `https://staging.roots-in-blue-stone.pages.dev`. Direct uploads must still be built separately: staging with `NEXT_PUBLIC_SITE_URL=https://staging.roots-in-blue-stone.pages.dev`, production with `NEXT_PUBLIC_SITE_URL=https://www.rootsinbluestone.com`. The `rootsinbluestone.com` custom domain is not attached to Pages yet, and DNS still needs cutover from Wix after the Pages custom-domain setup is completed.

2026-07-13 Wix contact audit: `/Users/llphant/Downloads/contacts.csv` was compared against the Roots In Blue Stone Mailchimp audience. The file had 213 rows, 42 valid unique email addresses, and 31 contacts marked `Subscribed`; 9 `Never subscribed` and 2 `Unsubscribed` contacts were skipped. All 31 subscribed Wix contacts were already in Mailchimp, so no contacts were added.

2026-07-14 booking delivery audit: Cloudflare Pages production secrets for `roots-in-blue-stone` currently include the Mailchimp newsletter secrets only; no `MAILCHIMP_TRANSACTIONAL_API_KEY`, `MANDRILL_API_KEY`, or `RESEND_API_KEY` is configured, so live booking submissions cannot deliver email yet. `app/api/book/route.ts` now returns HTTP 503 with the direct booking email instead of fake success when delivery is unconfigured, and `components/booking/booking-form.tsx` surfaces that API message to users. Mailchimp Marketing API keys ending in a data center suffix such as `-us22` are for newsletter/audience work and are not Mailchimp Transactional/Mandrill keys. Use Mailchimp Transactional/Mandrill plus verified `BOOKING_FROM_EMAIL`, or Resend fallback, before claiming booking email works.

2026-07-14 booking event taxonomy: visible booking form event-type buttons are now `Public Event`, `Private / Corporate Event`, `Wedding`, `Fundraiser`, and `Other`. The quote calculator maps Public Event to public-show pricing, Private / Corporate Event to private pricing, Fundraiser to the former festival/fundraiser pricing bucket, and Wedding to wedding pricing. Fundraiser estimates use that base fundraiser range with no extra discount. `Not sure — recommend one` lineup estimates intentionally span the calculated Duo-through-7-Piece range for the selected scenario. The live quote panel is hidden until the user reaches the Performance step and selects a lineup, so the form does not open with a misleading default public Duo estimate. The yellow potential-conflict warning for booked dates appears immediately under the Event Date field on the Event step, separate from the delayed quote panel. Conflict checks must read the website tour dates from `lib/tour.ts`, so future Bandsintown listener updates automatically feed both the public tour list and booking form. Set-length options are `≤ 2 hours`, `3 hours`, and `4 hours`; 4 hours is the max and the old integer custom-hours field is removed. The legacy `2 hours` label is still accepted, legacy `customHours` validates only at exactly 4, and repertoire no longer changes pricing; Original Music only caps effective set length at `≤ 2 hours`. Longer set lengths raise both min and max; Public Event Duo anchors are `$500-$1,200`, `$750-$1,500`, and `$1,000-$2,000`. The sound question label is `Will the band be providing sound?`; visible sound options are `Venue provides sound`, `Band provides sound`, and `Mix of Both`; these selections are informational only and do not change quote ranges. Legacy `Band provides PA / sound` and `Unsure` remain valid for stale browser tabs. The backline/gear selector only shows for `Venue provides sound` or `Mix of Both`, with both `Full PA / Sound System` and `Partial PA / Sound System` gear options. The final booking step only asks for open-ended notes; Budget Range, Travel & Lodging, Formal / Upscale Dress Requirement, and How Did You Hear About Us are intentionally not visible and not used by quote/email logic. Form submission is guarded so accidental submits before Step 4 only advance the flow and never call `/api/book`.
